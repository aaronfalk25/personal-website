import React from "react";
import { Element } from "react-scroll";

export const Resume: React.FC = () => {
    return (
        <Element name="resume">
            <h1>Resume</h1>
            <p>
                Section here to click to download resume.
            </p>
        </Element>
    );
}

export default Resume;